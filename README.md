# Global Manifold Audit (GMA-DTA)

Mass-scale validation of APS v2 against deterministic simulated diagnostic meta-analyses.

- Script: `simulation.py`
- Outputs: `certification.json`, `audit_results.csv`
- Default behavior: 100 simulations with a fixed seed and project-relative output paths
